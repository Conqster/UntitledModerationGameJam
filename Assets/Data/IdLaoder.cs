using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IdLaoder : MonoBehaviour
{
    // Start is called before the first frame update
    private ID newId;


    public void loadID(ID id)
    {
        newId = null;
        newId = id;

    }
}
